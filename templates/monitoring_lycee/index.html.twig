{% extends 'base.html.twig' %}

{% block title %}Hello MonitoringLyceeController!{% endblock %}

{% block body %}

<div class="card">
  <div class="card-body">
    {{ user.email }}
  </div>
</div>

<div class="card">
  <div class="card-body">
        <ul>
            {% for lyceeUser in lyceeUsers %}
                <li>{{ lyceeUser.email }}</li>
            {% endfor %}
        <ul>
  </div>
</div>
{% endblock %}
